= form_for(@board) do |f|
  .field
    = f.label :title
    = f.text_field :title, class: "form-control"
    = f.hidden_field :user_id, value: current_user.id
  .button-field
  = f.submit "#{t '.board_create'}", class: "btn btn-primary"
